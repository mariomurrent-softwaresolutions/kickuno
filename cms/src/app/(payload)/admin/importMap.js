// No custom admin components yet, so this stays empty. Once any collection or
// global config references a custom component, run `payload generate:importmap`
// to regenerate this file instead of hand-editing it.
export const importMap = {};
